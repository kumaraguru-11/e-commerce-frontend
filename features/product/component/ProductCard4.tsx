import React from "react";
import "./Card.css"; // Import the CSS file

const ProductCardFour = () => {
  return (
    <div className="card">
      {/* You can add content here, like an image or text,
          which would be constrained by the clip-path shape.
          The video mentions adding an <img> tag inside.
      <img src="..." alt="Content inside card" />
      */}
    </div>
  );
};

export default ProductCardFour;
